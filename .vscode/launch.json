{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Jekyll preview",
      "type": "node-terminal",
      "request": "launch",
      "cwd": "${workspaceFolder}",
      "command": "podman run --rm -e BUNDLE_PATH=/site/vendor/bundle -p 4000:4000 -v \"${workspaceFolder}:/site:Z\" -w /site docker.io/library/ruby:3.2-bookworm bash -lc 'bundle exec jekyll serve --host 0.0.0.0 --port 4000'",
      "serverReadyAction": {
        "pattern": "Server address: .*",
        "uriFormat": "http://127.0.0.1:4000",
        "action": "openExternally"
      }
    }
  ]
}
